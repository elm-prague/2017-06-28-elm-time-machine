# 2017-06-28-elm-time-machine
## Time machine - example app in Elm
### Run application

Run following commands, the application will start with hot loading.

`elm-package install`

`npm install`

`npm run dev`
